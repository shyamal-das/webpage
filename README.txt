Add these two files here (exact filenames matter):

1. cover.jpg   - wide background image for the top banner (used on every page)
2. profile.jpg - your headshot, shown on the Home page

Once added, refresh the site and they'll appear automatically.
Recommended: cover.jpg at least 1600px wide; profile.jpg roughly square (e.g. 800x800).
